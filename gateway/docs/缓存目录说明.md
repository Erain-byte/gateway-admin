# Gateway 降级缓存目录说明

## ✅ 修改完成

缓存目录已从项目根目录迁移到 **Gateway 项目内部**。

---

## 📁 缓存目录位置

### 新路径（修改后）

```
D:\python_project\gateway\data\
```

**特点**:
- ✅ 位于 Gateway 项目内部
- ✅ 独立于其他项目
- ✅ 便于管理和清理
- ✅ 已添加到 .gitignore

---

## 📂 缓存文件

### 1. Config Cache (配置缓存)

**文件**: `D:\python_project\gateway\data\config_cache.json`

**用途**: 存储从 Redis/Consul 降级的配置数据

**内容示例**:
```json
{
  "config:cors": "{\"origins\": [\"http://example.com\", \"http://localhost:9527\"], ...}",
  "config:hmac:test-app": "test-secret-key"
}
```

**代码位置**: `gateway/app/services/config_manager.py:38`

```python
self._cache_file = os.path.join(BASE_DIR, "data", "config_cache.json")
```

---

### 2. Services Cache (服务发现缓存)

**文件**: `D:\python_project\gateway\data\services_cache.json`

**用途**: 存储从 Redis/Consul 降级的服务发现数据

**代码位置**: `gateway/app/services/discovery.py:26`

```python
self._cache_file = os.path.join(BASE_DIR, "data", "services_cache.json")
```

---

## 🔧 修改内容

### 1. 修改 BASE_DIR 定义

**文件**: `gateway/config/settings.py`

**修改前**:
```python
# 项目根目录
BASE_DIR = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
# 结果: D:\python_project
```

**修改后**:
```python
# Gateway 项目根目录（当前文件所在目录的上级）
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
# 结果: D:\python_project\gateway
```

---

### 2. 创建 data 目录

```bash
cd d:\python_project\gateway
mkdir data
```

---

### 3. 更新 .gitignore

**文件**: `gateway/.gitignore`

**添加**:
```
data/
```

---

### 4. 迁移旧缓存文件

```bash
# 从项目根目录迁移到 Gateway 目录
Copy-Item "d:\python_project\data\config_cache.json" "d:\python_project\gateway\data\config_cache.json"
```

---

## 🧪 验证测试

运行测试脚本验证缓存功能：

```bash
cd d:\python_project\gateway
python test_cache_fallback.py
```

**测试结果**:
```
✅ 通过 - 缓存目录位置
✅ 通过 - 缓存保存和加载
✅ 通过 - 降级策略

总计: 3/3 测试通过
成功率: 100.0%

🎉 所有测试通过！降级缓存功能正常！
```

---

## 📊 降级策略流程

```
请求配置
    │
    ├─→ 1. 尝试从 Redis 获取
    │       ├─ 成功 → 返回数据
    │       └─ 失败 ↓
    │
    ├─→ 2. 尝试从 Consul 获取
    │       ├─ 成功 → 返回数据并缓存到 Redis
    │       └─ 失败 ↓
    │
    └─→ 3. 从本地缓存文件获取
            ├─ 成功 → 返回数据
            └─ 失败 → 返回 None
```

---

## 💡 优势

### 修改前（项目根目录）
```
D:\python_project\
├── gateway\
├── admin\
├── data\              ← 所有项目共享，容易冲突
│   └── config_cache.json
```

**问题**:
- ❌ 多个项目共享同一目录
- ❌ 容易产生命名冲突
- ❌ 清理时影响其他项目
- ❌ 不符合项目隔离原则

---

### 修改后（Gateway 内部）
```
D:\python_project\
├── gateway\
│   ├── app\
│   ├── config\
│   ├── data\          ← 独立目录
│   │   ├── config_cache.json
│   │   └── services_cache.json
│   └── ...
├── admin\
│   └── data\          ← Admin 可以有独立的 data 目录
```

**优势**:
- ✅ 每个项目独立管理
- ✅ 避免命名冲突
- ✅ 便于清理和维护
- ✅ 符合项目隔离原则
- ✅ 更容易理解和维护

---

## 🔍 验证缓存目录

### 方法 1: 命令行检查

```bash
cd d:\python_project\gateway
dir data
```

### 方法 2: Python 检查

```python
import os
from config.settings import BASE_DIR

cache_dir = os.path.join(BASE_DIR, "data")
print(f"缓存目录: {cache_dir}")
print(f"是否存在: {os.path.exists(cache_dir)}")
```

### 方法 3: 查看缓存文件内容

```bash
cd d:\python_project\gateway
Get-Content "data\config_cache.json" | ConvertFrom-Json | ConvertTo-Json
```

---

## ⚠️ 注意事项

### 1. 首次启动会自动创建

如果 `data` 目录不存在，ConfigManager 和 ServiceDiscovery 会自动创建：

```python
os.makedirs(os.path.dirname(self._cache_file), exist_ok=True)
```

### 2. Git 忽略

`data/` 目录已添加到 `.gitignore`，不会被提交到版本控制。

### 3. 备份建议

定期备份重要的缓存文件：

```bash
# 备份配置缓存
Copy-Item "data\config_cache.json" "data\config_cache.json.bak"
```

### 4. 清理缓存

如果需要清理缓存，直接删除文件即可：

```bash
# 删除配置缓存
Remove-Item "data\config_cache.json"

# 删除服务缓存
Remove-Item "data\services_cache.json"

# 或删除整个 data 目录
Remove-Item "data" -Recurse
```

下次启动时会自动重建。

---

## 📝 相关代码

### ConfigManager

```python
class ConfigManager:
    def __init__(self):
        # 本地缓存文件路径
        self._cache_file = os.path.join(BASE_DIR, "data", "config_cache.json")
        self._local_cache: Dict[str, Any] = {}
        
        # 加载本地缓存
        self._load_local_cache()
    
    def _load_local_cache(self):
        """加载本地缓存"""
        try:
            os.makedirs(os.path.dirname(self._cache_file), exist_ok=True)
            if os.path.exists(self._cache_file):
                with open(self._cache_file, "r", encoding="utf-8") as f:
                    self._local_cache = json.load(f)
                    logger.info(f"已加载 {len(self._local_cache)} 个配置项到本地缓存")
        except Exception as e:
            logger.warning(f"加载本地配置缓存失败: {e}")
            self._local_cache = {}
    
    def _save_local_cache(self):
        """保存本地缓存"""
        try:
            os.makedirs(os.path.dirname(self._cache_file), exist_ok=True)
            with open(self._cache_file, "w", encoding="utf-8") as f:
                json.dump(self._local_cache, f, ensure_ascii=False, indent=2)
        except Exception as e:
            logger.error(f"保存本地配置缓存失败: {e}")
```

### ServiceDiscovery

```python
class ServiceDiscovery:
    def __init__(self):
        # 本地缓存文件路径
        self._cache_file = os.path.join(BASE_DIR, "data", "services_cache.json")
        self._local_cache: Dict[str, ServiceInstance] = {}
        
        # 加载本地缓存
        self._load_local_cache()
```

---

## 🎯 总结

✅ **已完成**:
1. 修改 BASE_DIR 指向 Gateway 项目目录
2. 创建 `gateway/data/` 目录
3. 迁移旧的缓存文件
4. 更新 .gitignore
5. 运行测试验证功能正常

✅ **验证结果**:
- 缓存目录位置正确
- 缓存保存和加载正常
- 降级策略工作正常

✅ **优势**:
- 项目隔离，避免冲突
- 便于管理和维护
- 符合最佳实践

---

**降级缓存目录已成功迁移到 Gateway 项目内部！** 🎉
